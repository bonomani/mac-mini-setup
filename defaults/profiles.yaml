profiles:
  - id: configured
    label: Configured
    aliases: config|precondition|nonruntime
    axes: installation_state,health_state,dependency_state
    expected_text: installation=Configured health=Healthy dependencies=DepsReady
    installation: Configured
    runtime: Stopped
    health: Healthy
    admin: Enabled
    dependencies: DepsReady
  - id: runtime
    label: Runtime
    aliases: service|software
    axes: installation_state,runtime_state,health_state,dependency_state
    expected_text: installation=Configured runtime=Running health=Healthy dependencies=DepsReady
    installation: Configured
    runtime: Running
    health: Healthy
    admin: Enabled
    dependencies: DepsReady
  - id: capability
    label: Capability
    aliases: probe
    axes: health_state,dependency_state
    expected_text: health=Healthy dependencies=DepsReady
    installation: Configured
    runtime: Stopped
    health: Healthy
    admin: Enabled
    dependencies: DepsReady
  - id: parametric
    label: Parametric
    aliases: configured-value
    axes: installation_state,health_state,dependency_state,config_value
    expected_text: installation=Configured health=Healthy dependencies=DepsReady config_value=<as-declared>
    installation: Configured
    runtime: Stopped
    health: Healthy
    admin: Enabled
    dependencies: DepsReady
  - id: verification
    label: Verification
    aliases:
    axes:
    expected_text:
