# Git global config defaults — applied by component git
# Applied once when git-global-config is absent; skipped in non-interactive shells.

global_config:
  - key: init.defaultBranch
    value: main
  - key: pull.rebase
    value: "false"
  - key: core.autocrlf
    value: input
